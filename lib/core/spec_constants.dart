import 'package:flutter/material.dart';

class SpecConst {
  static const Color bg = Color(0xFFFFFDD0);
  static const Color primary = Color(0xFFFCE205);
  static const Color text = Color(0xFF333333);
  static const Color error = Color(0xFFFF6B6B);

  static const double radius = 24.0;
  static const double pad = 20.0;

  static const Duration jellyDuration = Duration(milliseconds: 520);
  static const Curve jellyCurve = Curves.elasticOut;
}
